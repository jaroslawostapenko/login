# Project Blueprint

## Overview
This is a React application built with Vite. The project is hosted on **GitHub Pages** with a custom domain `https://login.mqttwebs.com/`.

## Current State
- **Framework:** React + Vite
- **Hosting:** GitHub Pages
- **Domain:** login.mqttwebs.com (CNAME present)
- **Deployment Status:** Initial setup incomplete (missing gh-pages).

## Plan
1.  **Configure GitHub Pages Deployment:**
    -   Install `gh-pages` package.
    -   Add `deploy` and `predeploy` scripts to `package.json`.
    -   Verify `vite.config.js` settings.
2.  **Development:** Implement features as requested.
